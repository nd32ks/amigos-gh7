{
  "name": "kinbridge-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kinbridge — an AI companion web app for the elderly with daily conversation journals.",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test 'tests/*.test.js'"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
