<?xml version="1.0" encoding="utf-8"?>
<!-- Selectable card: hairline card with a soft violet ripple -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android" android:color="#1A5F79FF">
    <item android:drawable="@drawable/bg_card" />
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/pure_white" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</ripple>
